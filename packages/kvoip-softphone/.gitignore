# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
.idea/
*.lock

# Platform (gerado por `flutter create`)
# android/, ios/, web/, windows/, linux/, macos/ — versionados após create

# Env local
.env
.env.local
